import {createStore} from '@reduxjs/toolkit';
import productReducer from '.';

describe('Product store', () => {
  it('stores the products', () => {
    // set the store
    // const initialState = {
    //   products: [],
    // };
    // const store = createStore(productReducer, initialState);
    // dispatch loadProducts
  });
});
